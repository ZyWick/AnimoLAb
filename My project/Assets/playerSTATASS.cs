using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class playerSTATASS : MonoBehaviour
{
    public TMP_Text mesewqsage;
    // Start is called before the first frame update
    void Start()
    {
        mesewqsage  = gameObject.GetComponent();
        mesewqsage.SetText("hellodsad");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
